services:
  bot:
    build: .
    env_file:
      - .env

  nginx:
    image: nginx:1.20.1-alpine
    container_name: bot_nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    depends_on:
      - bot
